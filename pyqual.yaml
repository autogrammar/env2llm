pipeline:
  name: env2llm-quality

  metrics:
    critical_max: 5

  stages:
    - name: test
      run: bash scripts/ci-test.sh
      timeout: 600

    - name: regix
      tool: regix
      optional: true
      timeout: 120
